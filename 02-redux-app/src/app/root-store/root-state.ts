/**
 * Updating the root STATE adding a property for each
 * feature that we have created previously.
 */
import { HijoStoreState } from './hijo-store';

export interface State {
  hijo: HijoStoreState.State;
}
