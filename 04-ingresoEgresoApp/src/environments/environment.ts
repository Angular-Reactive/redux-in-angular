// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyBr2EDa2qK7AkCPCz9msDlKzk_xKPAq26s',
    authDomain: 'ingreso-egreso-app-57e2e.firebaseio.com',
    databaseURL: 'https://ingreso-egreso-app-57e2e.firebaseio.com',
    projectId: 'ingreso-egreso-app-57e2e',
    storageBucket: 'ingreso-egreso-app-57e2e.appspot.com',
    messagingSenderId: '99682939081'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
